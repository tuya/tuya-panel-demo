import React from 'react';
import { SwitchButton, SwitchButtonProps } from 'tuya-panel-kit';

const BoolView: React.FC<SwitchButtonProps> = props => <SwitchButton {...props} />;

export default BoolView;
