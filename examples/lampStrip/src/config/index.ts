import registerTheme from './theme';
import dpCodes from './dpCodes';
import dragonConfig from './dragon';
import panelConfig from './panelConfig';

export const CloudTimingCategory = 'timer_ordinary';

export { dpCodes, registerTheme, dragonConfig, panelConfig };
